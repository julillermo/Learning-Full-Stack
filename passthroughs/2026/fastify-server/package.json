{
  "name": "fastify-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "prebuild": "rimraf --no-verbose --no-interactive ./build",
    "dev": "concurrently -k --names tsc-watch,node-watch 'tsc --watch' 'node --watch ./dist/index.js' ",
    "start": "node ./dist/index.js ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.16.1",
  "dependencies": {
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "concurrently": "^9.2.1",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3"
  }
}
